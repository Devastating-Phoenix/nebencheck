// Placeholder to prevent `flutter create .` from generating the default
// counter-app test. The real logic tests live in analyzer_test.dart.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
